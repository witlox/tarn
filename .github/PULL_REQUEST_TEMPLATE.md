## Summary

<!-- What does this PR do? 1-3 bullet points. -->

## Motivation

<!-- Why is this change needed? Link to an issue if applicable. -->

## Changes

<!-- List the key changes. -->

## Testing

- [ ] `swift test` passes (172+ tests)
- [ ] `swift build --target TarnCore` succeeds
- [ ] `swift build --target TarnCLI` succeeds
- [ ] SwiftLint passes (`swiftlint lint --strict`)
- [ ] New code has tests
- [ ] Tested on macOS 14+ (if touching ES/NE code, tested on SIP-disabled VM)
